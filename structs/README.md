# Bmad structure parser

- Parses bmad Fortran code and generates `.yaml` details about all defined structures it found
- Generates JSON-Fortran code for dumping arbitrary ("raw") Fortran structures as JSON

## Usage

Installation of the Python package is not required.

To regenerate the structure information, use `regenerate.sh`.
